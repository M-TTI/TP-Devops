services:
  db:
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-shoplite_staging}

  api:
    environment:
      NODE_ENV: staging
      APP_VERSION: ${APP_VERSION:-staging}
      LOG_LEVEL: debug

  proxy:
    ports:
      - "${HTTP_PORT:-8081}:80"